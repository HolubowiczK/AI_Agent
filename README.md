Simple AI agent for learning.
Use Gemini API.

When a user asks a question or makes a request, make a function call plan. Can perform the following operations:
- List files and directories in workspace folder
- Read file contents
- Execute Python files with optional arguments
- Write or overwrite files

Need to create .env file with "GEMINI_API_KEY" value